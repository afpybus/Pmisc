# Convert named numeric vector to data frame

Convert named numeric vector to data frame

## Usage

``` r
named_num_to_df(named_numeric)
```

## Arguments

- named_numeric:

  A named numeric vector

## Value

Tibble with names and values columns
